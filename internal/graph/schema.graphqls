type Product {
  id: ID!
  name: String!
  price: Float!
}

type Query {
  products(limit: Int!, minPrice: Float, maxPrice: Float): [Product!]!
  product(id: ID!): Product
}

type Mutation {
  createProduct(name: String!, price: Float!): Product!
}